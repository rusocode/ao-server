# Ignore the folder /target and in the project root
target

# Ignore all the files and folders that begin with a point (hidden files)
.*

# Don't ignore .gitignore itself
!.gitignore

charfiles/*
!charfiles/.gitkeep